---
- name: Aws Ansible - Python project 
  # hosts: ubuntu
  # Use the public IP if the VM is already created
  hosts: ubuntu
  user: ubuntu
  become: true
  become_method: sudo
  become_user: root
  roles:
    - setup